/*
Reescribe este if usando el operador ternario '?':

if (a + b < 4) {
  result = 'Por debajo';
} else {
  result = 'Por encima';
}
*/
let a=2;
let b=5;
let result = (a + b < 4) ? 'Por debajo' : 'Por encima';
console.log(result);
