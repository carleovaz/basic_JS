/*
Escribe el código cambiando el bucle for por while sin alterar su comportamiento (la salida debe ser la misma).

for (let i = 0; i < 3; i++) 
{
  alert( `número ${i}!` );
}
*/
let i=0;
while(i<3)
{
    i++;
    alert(i);
}