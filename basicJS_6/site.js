/*
Escribe una condición if para comprobar que edad NO está entre 14 y 90 inclusive.

Crea dos variantes: la primera usando NOT !, y la segunda sin usarla.
*/


let edad=prompt("¿Cual es tu edad?");
if (!(edad >= 14 && edad <= 90));
if (edad < 14 || edad > 90);